int try(int c, int d) {
    write(c);
    write(d);
    return 0;
}

int test(int a, int b) {

    write(read());
    write(read());

    try(a, b);

    write(a);
    write(b);

    return 0;
}

int main() {
    int x, y;
    x = read();
    y = read();

    test(x, y);

    return 0;
}
